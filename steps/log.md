# Log

This file is split by task. Every Drawing Sheet entry is in `steps/log-drawing.md` and
every KPI entry is in `steps/log-kpi.md`, each newest first. The passes before the tools
split into two sessions are with Drawing Sheet, because that session wrote them. Nothing
writes here any more. A new task starts `steps/log-<task>.md` on its first round.
