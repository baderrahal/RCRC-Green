# Running the 154 again, 15 September 2026

For Bader, on Windows, in the order they happen. One action a step, and where a step carries a
command that is the one to paste. **Nothing here was run in Revit by anybody**, so every check
below is a check and not a claim.

What landed since the 13:32 press, all seven of your decisions of 15 September:

1. A shrubs and lawn schedule that was READ and printed no group writes 0 rather than leaving a
   box blank. 81 plots lost their Lawn box and 15 lost all four shrub boxes on that press.
2. A plot with NO softscape schedule leaves its three tree boxes and Total areas to be greened
   blank and says why. FM-07 read 0, 0, 0 and 0 against 57 trees in the model.
3. The Parks form's own boxes are 9.72 pt tall, so the height rule takes 1 pt off the top and
   1 pt off the bottom rather than 2 and 2. 271 values were held at 6 on that press.
4. A new species is never written into a row with no canopy formula. That is what blanked
   FP-23's Total areas to be greened.
5. Trees written nowhere show in THE PLOT LIST and in the glance. 67 of them went out silently.
6. No printed line anywhere says client. Every one of them names the file, the sheet, the row
   and the cell instead.
7. After a crash the tool leaves every file where it is and the plot's row names them.

---

## 1. Put the team's Revit fixes in the central model

**DO THIS FIRST AND DO NOT SKIP IT.** Whatever the team changed in Revit after the 13:32 press
goes into the CENTRAL model and is synchronised, or this run reads the old model and every
number below is about a file nobody will use again.

In Revit, **Collaborate** then **Synchronize and Modify Settings**, then **OK**. Wait for it to
finish before anything else.

## 2. Take a fresh detached copy

**File** then **Open**, pick the central model, tick **Detach from Central**, and choose
**Detach and preserve worksets**. The 13:32 press ran on
`RCRC_NG05_NU_MAIN_RVT24_SHEETS_detached`, which is a copy taken before the fixes, so a rerun on
that same file measures nothing new.

Close every other model first, so the pane cannot read the wrong one.

## 3. Pull main

In GitHub Desktop, with RCRC-Green open, press **Fetch origin** and then **Pull origin**. The
branch selector must read **main** before you pull.

## 4. Close Revit

Close Revit 2024 completely. The install overwrites the add-in's assemblies and Revit holds them
open while it is running.

## 5. Open the repository in VS Code

Open VS Code, then **File** then **Open Folder**, and choose the RCRC-Green folder GitHub Desktop
keeps.

## 6. Open a terminal in VS Code

**Terminal** then **New Terminal**. It opens in the repository root, which is where both commands
below expect to be.

## 7. Build it in Release

The install script reads `src\RcrcGreen.Revit\bin\Release`, so Release is what has to be built.

```
dotnet build RcrcGreen.sln -c Release
```

It must end with `Build succeeded.` and `0 Error(s)`. If it does not, stop here and send the
output.

## 8. Install it

```
.\install\install.ps1
```

It builds the `Addins\2024\RcrcGreen\` layout that the build does not, so copying the output
folder by hand leaves Revit unable to find the assembly. It ends by printing every file it
copied and `Restart Revit 2024 and look for the RCRC Green tab.`

## 9. Check the plot list file is still there

The 154 plots, one per line, in the plain text file you saved outside this repository, at
something like:

```
C:\Users\<you>\Documents\RCRC\plot-list-154.txt
```

**It must never go into the repository.** It is the scope list and the repository is public.

## 10. Open the model in Revit

Start Revit 2024 and open the detached copy from step 2.

## 11. Open the KPI pane

**RCRC Green** tab, then **KPI Checklist**.

## 12. Point the pane at the plot list

In the template block, on the **Plot list** line, press **Browse** and choose the file from step
9. Under it the pane says how many plots it read, over how many lines.

## 13. Read the plots

In the **Plots** block press **Read this model**. Nothing heavy runs without a press, so the plot
list cannot be ticked until this has finished.

## 14. Tick every workbook row

**THIS STEP IS EASY TO MISS AND IT COSTS THE WHOLE RUN.** In the template block, tick every
workbook row, which on the templates folder is all seven. A plot only goes into a workbook whose
row is ticked AND whose template is settled: `Settled()` at
`src/RcrcGreen.Revit/Kpi/KpiPanel.cs:1304` drops every other row, and the split at `:1296` runs
over what it hands back. **A ticked plot belonging to no ticked row is written nowhere.**

**If a row asks which of the two park templates it is, answer it.** The two park templates share
their main sheet name, so a file that settles nothing is ticked with no template and arms nothing
until you say which.

**These ticks live in the pane and nothing writes them down.** Closing Revit loses them, so this
step comes round again on every fresh start of the pane.

## 15. Press Tick the list

In the **Plots** block, beside **Select all** and **Clear**, press **Tick the list**. It replaces
every tick with exactly the plots the file names and forgets every plot you ticked or unticked by
hand, the same as those two do.

**THIS MUST BE THE LAST TICK ACTION BEFORE CREATE.** Ticking a template row ticks every plot that
belongs to it and unticking one takes them off again, both of them moving the plot ticks this step
has just set, at `src/RcrcGreen.Revit/Kpi/KpiPanel.cs:1248`. So if you touch a workbook row after
this, press **Tick the list** again before you go on.

## 16. Read the named lines above Create

Before pressing anything, read the lines above the **Create** button. Every one of these is named
there rather than found in a report afterwards:

- a plot on the list the model does not name
- a plot listed twice, with both line numbers
- a line that is not a plot, with its line number
- a listed plot that would get no workbook or no PDF, with the reason

If a plot you expect is named there, fix it before pressing Create.

## 17. Press Create

Press **Create**. The progress window says which plot is being read and how far through it is.

---

# The six checks

One check a step. Each one is a thing to look at, not a thing to take on trust.

## 18. THE PLOT LIST holds 154 and every gap carries a reason

Open the report. **THE PLOT LIST** is the first section. Its row now reads:

```
plot | in the model | ticked | workbook | PDF | trees not written | why not
```

Check four things:

- the heading reads `(154)`
- the three counts at the bottom, `listed: 154`, `workbooks written:` and `PDFs written:`
- **every row whose workbook or PDF column reads NO carries a reason in its last column**
- **every row that reads YES and YES and is still not complete carries one too.** That is new:
  a plot on no softscape schedule now says so in the last column even though both files were
  written, and a plot that lost a tree says so in the trees not written column

A row reading `YES | YES | none |` with an empty last column is a plot that came out right.
Anything else is named.

## 19. MM-08 has an Irrigation water demand

Open MM-08's PDF and read **Irrigation water demand**. It must hold a number.

On the 13:32 press it was empty on MM-01, MM-06, MM-07, NS-23, MM-08, NS-28 and NS-38, because
one of the two schedules printed its heading row and no rows under it, and the whole box refused
rather than counting that half as 0. A schedule holding nothing has nothing to irrigate.

**A schedule with rows and no TOTAL row still refuses**, which is unchanged: the species rows are
not added up instead.

## 20. One Parks PDF, at 7.7 pt with no runs over line

Open the PDF of any one EP or FP plot, which are the two that reach the **Parks** form. Read the
vegetation numbers: **Existing Trees, Proposed Trees, TOTAL trees**, the shrub rows under them,
and **Area**.

Then find **THE TEXT SIZES** near the top of the report and read that plot's lines under it.

- **Expect about 7.7 pt** on a Parks value box, not 6. The boxes are 41.52 by 9.72 pt, and the
  height rule takes 1 pt off the top and 1 pt off the bottom now rather than 2 and 2.
- **Expect NO line saying those values run over.** 271 values were held at 6 and named on the
  13:32 press, all of them on `Projects Basic Data - Parks`. A value held at 6 now says whether
  its WIDTH or its HEIGHT held it, and it says it runs over only when the text really is wider
  than the box at 6 pt.
- The open spaces and roads forms are unchanged, 46.6 by 19.4 pt and 46.8 by 17.0 pt, both
  capped at 10.

## 21. FP-23 has a Total areas to be greened

Open FP-23's PDF and read **Total areas to be greened**. It must hold a number.

It was empty on the 13:32 press because the run wrote CONOCARPUS LANCIFOLIUS, 2 trees, into
`GRP_-_KPI_Checklist_-_DD_FUTURE PARKS.xlsx`, Tree List - Proposed, D85 and B85, and that row
carries C85, M85 and O85 and no L85 canopy formula. The canopy guard was right to refuse: a count
on that row computes no canopy and the green cover would be short.

**A new species now takes the next empty row that carries the canopy formula.** If no empty row
of that sheet carries one, the species is not written at all and the report names every row it
checked, with the file, the sheet and the formula it was looking for.

## 22. FP-17, FP-20 and FP-21 show their trees in THE PLOT LIST

Find those three rows in THE PLOT LIST. Their **trees not written** column must read:

```
FP-17    36 AZADIRACHTA INDICA
FP-21    25 AZADIRACHTA INDICA
FP-20     5 AZADIRACHTA INDICA
```

And **THE TREES WRITTEN NOWHERE** in the glance must count them: 66 trees on 3 plots, plus
whatever FP-23's UNKNOWN tree adds.

**THESE ARE STILL NOT WRITTEN AND THAT IS ON PURPOSE.** The FUTURE PARKS workbook holds
AZADIRACHTA INDICA on more than one row and nothing can say which one the count belongs on, so
the tool refuses rather than guessing. The refusal line names the file, the sheet and both cells
now, so somebody can open the template at those two rows and delete one.

**Until the team fixes the template these rows stay.** What they must not do is disappear.

## 23. No line anywhere says client

Search the report for the word **client**. There must be no match, in any case.

Then look over the KPI pane's own lines, the tooltips on the two Browse buttons included. No line
there says it either.

Every one of those 26 lines now names the thing instead: the file, the sheet, the row and the
cell for a workbook, and the form title and the field name for a PDF. The canopy guard line names
the cell that would have carried the canopy and whether it is empty or holds a typed number.

---

# What remains

## Known, and not fixed by this round

**The FUTURE PARKS workbook holds AZADIRACHTA INDICA on more than one row.** The tool does not
choose between two rows and will not. That is the team's fix to the template and step 22 is how
you watch it.

**A plot with a tree on rows 85, 92 or 99 of a tree list still cannot use those rows.** They
carry the numbering column and nothing else, so the sheet computes no canopy from a count written
there, for this tool and for a person typing it in by hand alike. The tool now steps past them to
the next usable row instead of writing into them, which is better, but the rows are still dead
and the team's fix is still the answer.

**The canopy guard names the canopy cell and not the cell right of it.** It says `L85 is empty`
or `L85 holds 50 and no formula`, because the canopy column can be read off the sheet's own other
rows. **What M85 is, is UNKNOWN here.** Nothing in this tool records what that column's formula
is, so naming it would be a rule nobody measured. If you want that line too, send one row's M
cell and it becomes a measurement.

**The component and prefix refusal on MM-09 to MM-15 and FP-27 stays.** Where the two disagree
neither decides, which is the rule.

**Rows 85 to 101 of the templates are the team's fix**, unchanged.

**The `/DR` and `/Font` inline open item stays logged.** The 13:32 press measured every font and
came back with 0 widths UNKNOWN, so nothing has forced the question yet.

## The audit files

**80 findings across the four audit files, 20 of them marked FIXED, so 60 are open.** Counted on
15 September, per file:

```
steps/audit-kpi.md      29 findings   8 FIXED   21 open
steps/audit-kpi-2.md    20 findings   9 FIXED   11 open
steps/audit-kpi-3.md    14 findings   2 FIXED   12 open
steps/audit-kpi-4.md    17 findings   1 FIXED   16 open
                        80            20        60
```

Nothing was closed, renumbered or reordered in this round. A block of them is what the next round
can take.

## What comes next

1. Run it on the 154 and send what the list below asks for.
2. Take the FUTURE PARKS duplicate row to the team, with the two cells the report names.
3. Take rows 85, 92 and 99 to the team.
4. Send one Parks form's M cell if you want the canopy area column named in the guard line.
5. The next round can take a block of the 60 open audit findings.

---

# What to send back after the press

**All of it in one message.** Five things, and the report is open in front of you for the first
four.

1. **THE PLOT LIST**, its **three counts** at the bottom, and **every row whose last two columns
   hold anything**, which is every plot that lost a tree and every plot with a reason.
2. **THE TREES WRITTEN NOWHERE**, the glance line and the plots under it.
3. **THE TEXT SIZES**, the line itself and **every box line under it**. Those are the fields held
   at 6, the fields whose font could not be measured and the fields with no `/DA`.
4. **Whether the word client appears anywhere in the report**, yes or no.
5. **Screenshots of three PDFs**: MM-08, FP-23, and the EP or FP plot from step 20.

**Do not send the workbooks or the PDFs themselves**, and do not put any of them, or the plot list
file, or the report, into the repository. It is public.
