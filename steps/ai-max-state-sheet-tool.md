# ai-max state, Sheet Tool

Phase: 1, decide. Waiting on the user. This round wrote `steps/sheet-tool-phase1.md`, which
holds eleven questions and no answers to them, three candidate definitions of done differing
in kind rather than wording, what Shared offers and the one gap in it, and which of Drawing
Sheet's hard won rules apply here. Question one is where the line falls between Drawing Sheet
and this panel, and it is the user's to answer because a second panel overlapping the first
is worse than no second panel. `ReportPlaces` is in Drawing Sheet's folder rather than Shared
and View Filters already calls it across the fence, so a report from Sheet Tool needs a
Shared round the user schedules. `territory.md` is behind the repo and does not mention View
Filters, which is live. `SheetTool` is added to the hook's TASKS list, which a new task does
on its first round. No code, and the suite is unchanged at 1569.

Nothing moves until parts 1 to 3 of the phase 1 file are answered. The next round after that
is building, because the language and the harness are already settled by the three panels
that exist.
