{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo 'Read .claude/skills/ai-max/SKILL.md before doing any work in this repo.'"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Write|Edit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'H=\"$CLAUDE_PROJECT_DIR/.claude/hooks/block-paths.sh\"; if [ ! -x \"$H\" ]; then echo \"Refused. The hook $H is missing or not executable, so this write was not checked.\" >&2; exit 2; fi; exec \"$H\"'"
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'H=\"$CLAUDE_PROJECT_DIR/.claude/hooks/require-file-on-commit.sh\"; if [ ! -x \"$H\" ]; then echo \"Refused. The hook $H is missing or not executable, so this command was not checked.\" >&2; exit 2; fi; exec \"$H\"'"
          },
          {
            "type": "command",
            "command": "sh -c 'H=\"$CLAUDE_PROJECT_DIR/.claude/hooks/territory-check.sh\"; if [ ! -x \"$H\" ]; then echo \"Refused. The hook $H is missing or not executable, so this command was not checked.\" >&2; exit 2; fi; exec \"$H\"'"
          },
          {
            "type": "command",
            "command": "sh -c 'H=\"$CLAUDE_PROJECT_DIR/.claude/hooks/writing-check.sh\"; if [ ! -x \"$H\" ]; then echo \"Refused. The hook $H is missing or not executable, so this command was not checked.\" >&2; exit 2; fi; exec \"$H\"'"
          }
        ]
      }
    ]
  }
}
